services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=circuit_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d circuit_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  gateway:
    build:
      context: .
      dockerfile: Dockerfile
      target: gateway-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app:z
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3000
      - AUTH_SERVICE_URL=http://auth:3001
      - GEO_SERVICE_URL=http://geo:3002
      - SOCIAL_SERVICE_URL=http://social:3003
    depends_on:
      - auth
      - geo
      - social

  auth:
    build:
      context: .
      dockerfile: Dockerfile
      target: auth-dev
    ports:
      - "3001:3001"
    volumes:
      - .:/app:z
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3001
      - DATABASE_URL=postgresql://postgres:password@db:5432/circuit_db
    depends_on:
      db:
        condition: service_healthy

  geo:
    build:
      context: .
      dockerfile: Dockerfile
      target: geo-dev
    ports:
      - "3002:3002"
    volumes:
      - .:/app:z
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3002
      - DATABASE_URL=postgresql://postgres:password@db:5432/circuit_db
    depends_on:
      db:
        condition: service_healthy

  social:
    build:
      context: .
      dockerfile: Dockerfile
      target: social-dev
    ports:
      - "3003:3003"
    volumes:
      - .:/app:z
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3003
      - DATABASE_URL=postgresql://postgres:password@db:5432/circuit_db
    depends_on:
      db:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  postgres_data:
  redis_data:
