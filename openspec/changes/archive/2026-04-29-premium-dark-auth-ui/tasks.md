## 1. UI Component Updates

- [x] 1.1 Add `midnight` variant to `apps/mobile/src/components/ui/ThemeGradient.tsx` (Colors: `#0F172A` to `#000000`)
- [x] 1.2 Update `PremiumButton.tsx` to ensure `primary` variant works perfectly on dark backgrounds (Solar Gold gradient with black text)
- [x] 1.3 Add a `midnight` or `dark` prop to `AuthLayout.tsx` to automatically adjust text colors

## 2. Screen Transformations

- [x] 2.1 Update `welcome.tsx` to use `midnight` theme and primary/glass button combinations
- [x] 2.2 Update `login.tsx` to use the dark theme and high-contrast inputs
- [x] 2.3 Update `register.tsx` to match the new dark aesthetic

## 3. Polish & Verification

- [x] 3.1 Verify contrast ratios on the welcome screen buttons
- [x] 3.2 Ensure native blur (glass effect) is working correctly on the secondary buttons
- [x] 3.3 Test readability of the "Solar Gold" text on deep charcoal backgrounds
