## 1. Theme and Tokens Fixes

- [x] 1.1 Update `theme.ts` with increased opacity for `glass.subtle` and `subtleBorder` in light mode
- [x] 1.2 Update `theme.ts` dark mode `glass.subtle` tokens for consistency

## 2. Component Refactoring

- [x] 2.1 Refactor `CategoryChip.tsx` to ensure visibility and proper spacing
- [x] 2.2 Update `SearchFilters.tsx` to ensure layout stability in the bottom sheet
- [x] 2.3 Refactor `POICarousel.tsx` to fix card aspect ratios and theme compatibility
- [x] 2.4 Update `GuidesSection.tsx` to ensure consistent discovery content styling

## 3. Bottom Sheet Integration

- [x] 3.1 Update `MapBottomSheet.tsx` to ensure proper vertical spacing and prevent content compression
- [x] 3.2 Verify layout on both Light and Dark modes
