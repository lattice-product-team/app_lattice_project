## ADDED Requirements

### Requirement: Reliable Creation Flow

The system SHALL provide a reliable modal-based interface for creating events and POIs that remains responsive during the entire data entry process.

#### Scenario: Registering Event with Boundary

- **WHEN** the user defines an event boundary on the map within the creation modal
- **THEN** the UI MUST remain responsive to further text input and button clicks.
