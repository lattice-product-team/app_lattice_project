## 1. Foundation & Assets

- [ ] 1.1 Create `SplashScreen` component in `src/components/ui/` with basic layout.
- [ ] 1.2 Implement the animated gradient background using Reanimated or Skia.
- [ ] 1.3 Add the "Glass" logo card using `SafeBlurView`.

## 2. Core Logic

- [ ] 2.1 Implement the initialization orchestration logic (fonts, auth, minimum time).
- [ ] 2.2 Integrate the `SplashScreen` into `app/index.tsx` as the default loading view.
- [ ] 2.3 Add entrance and exit animations to the `SplashScreen`.

## 3. Integration & Refinement

- [ ] 3.1 Synchronize the splash exit with the `RootLayout` mount state and routing.
- [ ] 3.2 Ensure fallback behavior works when Skia is missing.
- [ ] 3.3 Fine-tune animation timings and easing to match the Jovi-inspired design.
