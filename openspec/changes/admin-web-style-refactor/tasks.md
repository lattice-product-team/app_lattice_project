## 1. Global Styles & Token System

- [x] 1.1 Define Tailwind v4 `@theme` variables in `globals.css` for admin typography (no hardcoded sizes).
- [x] 1.2 Implement a unified token system for Neutral/Surface colors in both **Light and Dark** modes.
- [x] 1.3 Ensure `Inter` font is applied globally using theme variables.

## 2. Component API Fixes (HeroUI v3)

- [x] 2.1 Refactor `apps/admin-web/src/app/page.tsx` (Dashboard) to use HeroUI v3 Dot Notation and fix accessibility.
- [x] 2.2 Refactor `apps/admin-web/src/app/events/page.tsx` to use Dot Notation and resolve all TypeScript build errors.
- [x] 2.3 Audit other HeroUI components for ARIA accessibility warnings and fix them.

## 3. UI Refinement & Theme Consistency

- [x] 3.1 Standardize Metric Card UI in `src/app/page.tsx` using theme tokens for both themes.
- [x] 3.2 Refine Light Theme aesthetics (Warm Light palette) for professional admin readability.
- [x] 3.3 Update Sidebar and Header navigation to use the new professional typography and token-based colors.
